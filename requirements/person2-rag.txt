# Indexation & Moteur RAG only. Needed for embeddings, vector store, and RealRAGService.
# This is exactly the set of heavy libraries that used to be forced on
# everyone. Now only the Indexation & Moteur RAG environment (and CI job, if any) installs it.
-r base.txt
sentence-transformers==3.1.1
chromadb==0.5.5
ollama==0.6.2
